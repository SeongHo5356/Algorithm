import sys

def maxIncreasingPermutation(numbers):
    PermutationSum = 0
    

    for i in range(len(numbers)):
        if numbers[i] < numbers[i+1]: 
            0
    

n = int(sys.stdin.readline())

